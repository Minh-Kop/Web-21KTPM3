<style>
    .category-page__breadcrumb {
        text-decoration: none;
        color: #2f2e2e;
    }

    .breadcrumb-item.active span {
        color: var(--secondary-color) !important;
        font-weight: bold;
    }
    .sidebar-section {
        background-color: white;
        padding: 10px;
    }
    
    .category-section,
    .price-section,
    .publisher-section {
        padding-bottom: 10px;
        margin-bottom: 10px;
        border-bottom: 1px solid var(--category-font-color);
    }

    .category-section__heading {
        font-size: 16px;
        font-weight: bold;
        text-transform: uppercase;
        {{!-- margin: 0; --}}
    }

    .category-section__list,
    .price-section__list,
    .publisher-section__list {
        margin: 0 0 0 10px;
        padding: 0;
        color: var(--category-font-color);
        font-size: 14px;
    }
    
    .category-section__list li {
        padding: 0;
    }

    .category-section__list__link {
        display: block;
        text-decoration: none;
        color: var(--category-font-color);
        font-size: 14px;
        text-transform: capitalize;
        margin-bottom: 7px;
    }

    .parent-active > .category-section__list__link,
    .child-active > .category-section__list__link,
    .active > .category-section__list__link {
        font-size: 15px;
        font-weight: bold;
        margin-bottom: 8px;
    }

    .active > .category-section__list__link,
    .child-active > .category-section__list__link {
        color: var(--secondary-color);
        cursor: text;
    }

    .child-active > .category-section__list__link {
        margin-bottom: 7px;
    }
</style>